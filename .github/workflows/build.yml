name: Build Release

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  GO_VERSION: '1.21'
  APP_NAME: roub-crt

jobs:
  # GUI版本构建
  build-gui:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            env: ''
            gui-deps: 'libgl1-mesa-dev xorg-dev libgtk-3-dev'
          - os: windows-latest
            env: '.exe'
            gui-deps: ''
          - os: macos-latest
            env: ''
            gui-deps: ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install GUI dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.gui-deps }}

      - name: Install GUI dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          xcode-select --install || true
          brew install glfw3 pkg-config
          mkdir -p /tmp/opengl/include/KHR
          curl -sL https://raw.githubusercontent.com/KhronosGroup/EGL-Registry/main/api/KHR/khrplatform.h -o /tmp/opengl/include/KHR/khrplatform.h
          curl -sL https://raw.githubusercontent.com/KhronosGroup/OpenGL-Registry/main/api/GL/glcorearb.h -o /tmp/opengl/include/GL/glcorearb.h
          echo "CGO_CFLAGS=-I$(brew --prefix glfw3)/include -I/tmp/opengl/include" >> $GITHUB_ENV
          echo "CGO_LDFLAGS=-L$(brew --prefix glfw3)/lib" >> $GITHUB_ENV

      - name: Get dependencies
        run: |
          go mod download
          go mod tidy

      - name: Build GUI
        run: |
          mkdir -p release/${{ matrix.os }}
          go build -tags gui -ldflags="-s -w" -o release/${{ matrix.os }}/${{ env.APP_NAME }}-gui${{ matrix.env }} .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-${{ matrix.os }}-gui
          path: release/${{ matrix.os }}/${{ env.APP_NAME }}-gui${{ matrix.env }}
          retention-days: 30

  # 创建发布
  release:
    needs: [build-gui]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Generate checksums
        shell: bash
        run: |
          cd artifacts
          find . -type f -name "*" -exec sha256sum {} \; > checksums.txt
          cat checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/**/*
            artifacts/**/checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
